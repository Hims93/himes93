#include<stdio.h>
int avg(int[],int);
int avg(int s[],int a) {

    int i,sum=0;

    for(i=0; i<a; i++)
    {
        sum=sum+s[i];
    }
    return (sum/a);
}

int main()
{
    int s[]= {10,20,30,40,50},a;
    a= sizeof(s)/sizeof(s[0]);
    printf("%d",avg(s,a));
    return 0;
}
